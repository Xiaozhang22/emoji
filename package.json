{
  "name": "emoji-gen-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for AI Emoji Generation via ModelScope",
  "main": "src/worker.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "wrangler deploy"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@cloudflare/ai": "^1.0.0"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
